# Veriqid Browser Extension (Phase 5)

Chrome extension (Manifest V3) for master secret key management.

## Planned Components

- **Popup UI:** Setup wizard, identities list, parent controls
- **Background worker:** Bridge API communication, encrypted storage
- **Content script:** Detect signup forms, auto-fill proofs, show Veriqid badge
